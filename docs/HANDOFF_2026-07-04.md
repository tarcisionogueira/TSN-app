# Handoff — BidPro Brasil — 2026-07-04

Branch de trabalho: `claude/bidprobrasil-handoff-8dmqaz` (deploy automático → `main` → Vercel produção `bidprobrasil.com.br`).
Supabase projeto `zuwfiwokkdytvjixiwac`.

## 1. O que foi resolvido hoje (tudo em produção)

### Dados / enriquecimento
- **Score de localização (OSM)** — reescrito para **extrato local** (Geofabrik Brasil + `osmium`), sem Overpass. `scripts/enriquecer-osm.mjs` + workflow `enriquecer-osm.yml` (diário 2h BRT). **3.803 imóveis** com score (todos os de nível endereço), média 4,72. Roda em ~4 min.
- **Datas CEF** — causa real era a Caixa devolver página de **erro 504** intermitente (lida como "sem data" e travando o imóvel). Corrigido: detecção de erro + re-tentativa + **disjuntor** anti-travamento + ritmo gentil + **cron a cada 3h**. De 478 → **852 e subindo** (a Caixa bloqueia o IP do runner após ~3 min/execução; o cron acumula ao longo do dia; alvo ~9.655).
- **Re-geocodificação de imprecisos** — `api/regeocod-imprecisos.js` (cron diário 6h UTC) marca em lotes os imóveis `bairro/cidade/falhou` como `refazer`, rotacionando por `geocod_reproc_em` (coluna nova) — sobe o % de nível endereço com o tempo.

### Resiliência (multi-rota)
- **IA (Claude)** — `api/_claude.js`: retry/backoff (429/500/502/503/529 + Retry-After) em **todas** as chamadas (13 endpoints). **Fallback OpenAI** pronto, acionado só se `OPENAI_API_KEY` existir e a requisição não usar web_search/tools (resposta no mesmo shape do Anthropic).
- **Geocoding** — retry/backoff nos provedores; **rota grátis nova BrasilAPI CEP→coordenada**; **tier pago pronto** (LocationIQ) atrás de `GEOCODER_KEY`.
- **Coleta CEF (Vercel)** — fallback **Bright Data** quando o fetch direto é bloqueado (`scraper-caixa.js`).
- **Bright Data** — sub-cota por propósito: LJUD ≤180/execução (`BRIGHTDATA_MAX_LJUD_SEMANA`), para não drenar o teto compartilhado. *Nota: contador é por processo/execução — limita o cenário real (paginação do LJUD numa invocação); o teto global de 450 permanece o limite duro persistido no banco.*
- **Captura do edital CEF** — best-effort durante o scrape de datas (inspeciona o DOM do controle "Baixar edital e anexos"); grava `link_edital` quando acha algo confiável e o registro está vazio. Resolve o "Edital no site" quando bem-sucedido.

### Score BidPro (novo modelo em camadas) — **aguardando sua validação**
Média **ponderada das camadas presentes** (re-normalizada), então já na busca chega a ~10 e refina após a análise:
`Margem 40 · Localização 25 · Perfil (modalidade/tipo) 15 · Jurídico 10 · Financeiro 10`.
Pesos e tabelas de perfil no topo de `src/utils/score.js` (fáceis de calibrar). Barras de sub-score no detalhe do imóvel.

### Filtros / imagens
- **Filtro de valor no raio** alinhado ao caminho sem-raio (mapa e lista concordam) — regra dos filtros.
- **Fotos nos popups do mapa** agora usam a mesma cascata dos cards (hotlink → padrão Caixa → proxy).

### Segurança do banco (advisors Supabase)
- **View `advogado_eficiencia`**: `SECURITY DEFINER` → `security_invoker=on` (era o único ERRO).
- **Funções** `proteger_campos_sensiveis_perfil` e `limite_ia`: `search_path` fixado em `public`.
- `sdr_leads` INSERT público confirmado **seguro por design** (só equipe/admin lê).

## 2. Estado atual (métricas)
- Imóveis ativos: **32.528**.
- Datas: leiloeiros ~100%; **CEF 852/9.655** (subindo via cron).
- Geocoding: **4.077** nível endereço · **26.258** imprecisos · 121 falhou.
- Score de localização: **3.803** (média 4,72).

## 3. Pendências que dependem de VOCÊ (chaves/config) — o código já está pronto
1. **Geocoding pago** (maior alavanca): criar conta LocationIQ/Geoapify/Mapbox e setar `GEOCODER_KEY` no Vercel → a cascata passa a usar o tier preciso automaticamente.
2. **IA de reserva**: setar `OPENAI_API_KEY` no Vercel → fallback OpenAI ativa (remove o SPOF de provedor único).
3. **Auth — proteção de senha vazada** (advisor WARN): Supabase → Authentication → Policies → habilitar "Leaked password protection" (toggle de 1 clique; não dá para migração SQL).

## 4. Itens conhecidos deixados para depois (documentados)
- Sort "Encerra em breve" no modo raio (precisa reordenar a RPC — evitado hack).
- `MapaImoveis.jsx` (mapa global) filtra só tipo+estado (não usa `aplicarFiltrosImoveis`).
- Captura de **PDF de edital para os leiloeiros** (hoje só CEF, best-effort).
- Rehospedar fotos no Supabase Storage (mata hotlink/403 de vez).
- Rotas 2 reais para ZUK/SODRÉ/FRAZÃO no cron Vercel (hoje rota única).
- Extensões `cube`/`earthdistance` em `public` (mover quebra a busca por raio — mantido de propósito).
- Bucket `imoveis-fotos` permite listagem (fotos públicas, risco baixo).

## 5. Como retomar amanhã
- Você vai **gerar relatórios de teste** e **validar o Score BidPro** (calibrar pesos em `src/utils/score.js` se quiser).
- Voltar às **validações de tela**.
- Se liberar as chaves do item 3, os ganhos de geocoding e IA entram sozinhos.

## Migrações aplicadas hoje (também salvas em `supabase/migrations/`)
`busca_raio_v2_desconto_min`, `busca_raio_v2_valor_null`, `busca_raio_v2_score_localizacao`, `add_score_localizacao`, `add_parecer_reuniao`, `add_geocod_reproc_em`, `seguranca_view_e_search_path`.
