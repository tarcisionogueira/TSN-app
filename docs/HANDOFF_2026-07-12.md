# Handoff — 2026-07-12 (BidPro Brasil / TSN-app)

> Abertura do dia. Continuação do HANDOFF_2026-07-11.md. Foco da sessão: **documentação
> dos leiloeiros** (matrícula/edital/regras) e correção em massa dos que tinham documentos
> pendentes. Detalhe completo por leiloeiro em **`docs/COBERTURA_DOCUMENTAL_LEILOEIROS.md`**.

## 🎯 Grande entrega da sessão — documentos dos leiloeiros

### Grupo Lance (o "0 anexos" que o dono apontou no print) — ✅ RESOLVIDO
- **Diagnóstico (sondas no Actions):** o enrich diário (`enriquecerDocumentosLote`, cap 150 +
  deadline 8 min) nunca alcançava ~254/399 lotes. E a estrutura é mista: **Edital/Processo são
  públicos** no CDN, mas **Matrícula/Auto de avaliação/Análise processual são login-gated**
  (endpoint Yii `/lote/baixar-documento/{id}/file_xxx`, `data-url` base64; anônimo → 302 `/entrar`).
- **Solução (molde ZUK adaptado ao login Yii):** `api/_grupolance-auth.js` + `scripts/captura-docs-grupolance.mjs`
  + `captura-docs-grupolance.yml` (4×/dia). Loga uma vez, **lê a página com sessão ANÔNIMA aquecida**
  (logado o GL esconde os `.doc-link`), baixa a matrícula pro Storage e resolve os demais docs pro CDN.
  Preenche `imoveis_leilao.anexos` (o campo que estava em 0).
- **Resultado:** 145→**393/399 com anexos**, matrícula 118→**348** (87%).

### Backfill dos demais leiloeiros públicos — ✅ MEGA e BIASI 100%
- **Recon primeiro** (`recon-docs-leiloeiro.yml`, Puppeteer, sem credencial): **nenhum dos testados
  bate em parede de login.** Os buracos são de cobertura/seletor.
- **Ferramenta nova:** `scripts/captura-docs-publico.mjs` + `captura-docs-publico.yml` (backfill
  público genérico, `PUB_FONTE`, concurrency por fonte).
- **MEGA:** 25% → **635/635 com anexos, matrícula 633 (99,7%)**.
- **BIASI:** 18% → **260/260 com anexos, edital 100%** (matrícula 44 — o site só publica em parte).
- **ZUK:** matrícula 6 → **156** (rodado `matricula-zuk.yml`, molde já existente). Teto = `url_lote`
  (scraper só gravou ~120/600).

## 🔑 ETAPA DE FECHAMENTO — credenciais + moldes de acesso — ✅ CONCLUÍDA (noite 12/07)
O dono cadastrou `GL_*`, `SODRE_*`, `FRAZAO_*` no **GitHub** e **Vercel** → **validados (presentes)**.
Descobertas e resultados:
- **Via de acesso por login (pedido central) — ✅ framework pronto** em `api/_leiloeiro-auth.js`,
  ligado na análise (`api/gerar-documental.js` chama `capturarDocsLoginOnDemand(fonte, loteUrl, deadline)`).
  Funcional hoje: **ZUK** (Laravel) e **GRUPO LANCE** (Yii), fetch/serverless. Encaixe pronto p/ os demais;
  fonte sem molde retorna null e a análise segue.
- **GRUPO LANCE** — ✅ `GL_*` ok; 87% matrícula (molde de manhã).
- **SODRE (35)** — ✅ **público** (navegador na nuvem revelou): API `search-lots` (ES, sem auth) dá
  lot_id+auction_id; PDFs em `arquivos.sodresantoro.com.br/anexos/{leilão}_{lote}_{doc}.pdf`. `url_lote`
  do banco estava 404; corrigida. `captura-docs-sodre.*`. **20/35** (15 são leilões encerrados/removidos).
- **FRAZAO (144)** — ✅ **público** (com a pista do print do dono): link "Consulte o edital e documentação"
  → `GET /Sale/LotDocs?loteId&leilaoId` (ASP.NET). PDFs em `cdn.frazaoleiloes.com.br/file/loteanexo|leilaoanexo/…`.
  `captura-docs-frazao.*` (fetch): decode de entidades HTML, `loteanexo`→matrícula, e contorno do **HTTP 429**
  (bloqueia ~27/sessão → para cedo + **schedule 3/3h** + re-dispatch). **0 → 143/144 com matrícula (distintas);
  1 restante drena no schedule.**
- **MEGA/BIASI** já eram públicos (resolvidos de manhã, 100%). **SODRE/FRAZAO** confirmaram: os "gated" que
  imaginávamos eram públicos — o navegador na nuvem serve para grampear o tráfego e descobrir isso.

## 📋 Pendências que restam (nenhuma precisa de credencial, exceto LJUD)
- **SODRE:** 15/35 são leilões encerrados (sem página) — nada a capturar; e o scraper mantém lotes
  encerrados como `ativo` (higiene de dados a rever, vale p/ FRAZAO também).
- **SOLD/SUPERBID/SBID9/SBID21 (~1.734):** o seletor pega um anexo genérico repetido → extrator da
  plataforma Superbid (doc por lote no JSON/estado da página). **Método:** mesmo recon de navegador do Sodré.
- **ZUK `url_lote`:** scraper só gravou ~120/600 → corrigir para destravar mais matrículas.
- **LJUD (1.046):** agregador aberto — **integração oficial** (comercial, dono conduzindo).
- **Regras de venda:** só CEF publica por lote; nos demais vive dentro do edital (vai junto quando
  o edital é capturado).

## 🧰 Artefatos criados nesta sessão
- `api/_grupolance-auth.js` — login Yii + coleta + download (molde reutilizável).
- `scripts/captura-docs-grupolance.mjs` + `.github/workflows/captura-docs-grupolance.yml`.
- `scripts/captura-docs-publico.mjs` + `.github/workflows/captura-docs-publico.yml` (backfill público genérico).
- `scripts/recon-docs-leiloeiro.mjs` + `.github/workflows/recon-docs-leiloeiro.yml` (recon público-vs-login).
- **`api/_leiloeiro-auth.js`** — fallback de login on-demand unificado (registro de adaptadores por leiloeiro).
- **`scripts/captura-docs-sodre.mjs`** + `captura-docs-sodre.yml` (Sodré público via search-lots).
- **`scripts/captura-docs-frazao.mjs`** + `captura-docs-frazao.yml` (Frazão público via Sale/LotDocs, schedule 3/3h).
- Recons de login/API: `recon-login-leiloeiro.*`, `recon-login-puppeteer.*`, `recon-sodre-api.*`,
  `recon-sodre-frazao.*`, `debug-frazao-modal.*` (o navegador na nuvem grampeia o tráfego → tira o molde).
- Sondas de diagnóstico: `debug-grupolance-puppeteer.*`, `debug-grupolance-login.*`, `debug-frazao-docs.*`.
- **`docs/COBERTURA_DOCUMENTAL_LEILOEIROS.md`** — matriz + plano + registro de credenciais + arquitetura Sodré/Frazão.

## ⏰ Cadência da auditoria (lembrete que segue ativo)
Plano combinado: 12/07 e 13/07 rodam diárias; **14/07 trocar cron para SEMANAL**
(`auditoria-claude.yml`: `0 6 * * *` → `0 6 * * 1`). Lembrete `trig_01Vx9LpTsHrzvdfwbxohEeYr` (14/07 11:00Z).

## Regras operacionais (lembrete)
- Deploy: `npm run build` → commit em PT → push em `main` (fast-forward do branch da sessão) → Vercel automático.
- Scrapers/capturas pesadas rodam no **GitHub Actions** (egress liberado; o sandbox bloqueia sites externos).
- Método sempre **recon-first** antes de assumir login: `recon-docs-leiloeiro.yml FONTE=<X>`.
- Prioridade do dono: **qualidade das funções > segurança de dados/fluxos/pagamentos > economia**;
  correções sempre **na raiz** (gerais), não lote a lote.
