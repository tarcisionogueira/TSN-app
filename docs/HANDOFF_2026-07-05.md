# Handoff — BidPro Brasil — 2026-07-05

Deploy: push em `main` → Vercel produção `bidprobrasil.com.br`. Supabase `zuwfiwokkdytvjixiwac`.
Branch de trabalho da sessão: `claude/handoff-review-6yykuk` (espelhado em `main`).

---

## 1. O que foi para produção hoje

### Criptografia de CPF — CONCLUÍDA (etapas 1 e 2)
- CPF **não existe mais em texto claro**: `perfis.cpf = 0` registros e `raw_user_meta_data.cpf = 0`. Só ficam **`cpf_hash`** (HMAC, dedup) e **`cpf_enc`** (AES-GCM). Chave `CPF_ENC_KEY` na Vercel (Production+Preview).
- Helper `api/_cpf.js`: `hashCpf`, `encryptCpf`, `decryptCpf`, `cpfDoRegistro` (decifra na saída), `maskCpf`.
- Saída/máquina decifra sob demanda: **NFS-e** (`_webhook-core`), **LGPD** (`lgpd-exportar`), **Asaas** e **Mercado Pago** (buscam `cpf_enc` do usuário autenticado, não confiam no body).
- Telas mascaram via `api/cpf-revelar` (autorizado): Admin/Usuários e Perfil mostram `•••.•••.XXX-••`; contratos/procuração (Caso, Contratos, OnrRegistro) recebem CPF cheio só para titular/jurídico.
- Gravações só hash+cifra: trigger `handle_new_user` (migração), `cpf-set`, checkout, ConviteEquipe. Backfill dos 4 CPFs existentes feito via botão no Dashboard.

### SDR — funil de captura ponta a ponta (novo, funcionando)
- `api/sdr-capturar.js`: cria conta confirmada, **sorteia** um consultor ativo, vincula o lead à **carteira** dele (`perfis.indicado_por`), concede acesso (explorador) e grava o lead com respostas. Página pública `/#/p/captura/:id` (perguntas uma-a-uma já existia).
- Corrigido bug que deixava a **carteira do consultor sempre vazia** (query pedia colunas inexistentes `email/whatsapp` em `perfis`).
- ⚠️ Hoje há **0 consultores ativos** → leads ficam no pool até cadastrar consultores.

### Admin/Usuários
- Role mostra o **nome oficial do plano** (`top2`→"Investidor Pro"), não a chave crua. Coluna "Plano" **derivada do role** com selo **PAGO** (a coluna `perfis.plano` está morta, vale 'gratuito' p/ todos).

### Auditoria de segurança
- Verificado: **0 críticos abertos** (os 40 achados eram de rodada ANTES dos fixes). Corrigidos hoje: **race de login ONR** (`_onr.js`, Promise única) e **Leilão Club R$5.000** mapeado como assessorado (`_webhook-core` casava só "clube", não "club").
- Deixados documentados (baixo risco): race de customer Asaas, prefixo `VITE_`, webhook MP fail-open (reconfere via API do MP).

### Página do imóvel — mapa
- Corrigido **mapa em branco**: reinvalida tamanho + `ResizeObserver` (remontava ao carregar pontos próximos com container 0px).

### Score BidPro — reflete a análise real (corrige "card bom × relatório reprovado")
- **Causa raiz:** Margem (40%) + Financeiro (10%) vinham do **desconto vs. AVALIAÇÃO da Caixa** (inflada). A análise real (valor de mercado + ROE) não voltava para o score.
- **Fix:** `gerar-analise` persiste no imóvel `valor_mercado` + `analise_viavel` (ROI≥30) — migração `imoveis_leilao_veredito_analise`. `score.js`: Margem usa **desconto real vs. mercado** quando há análise; imóvel **reprovado é rebaixado** (nota ≤3,5, vermelho). `Busca` e `ImovelDetalhe` passam os campos.

### Jurídico/documental — não gera sem documentos
- `gerar-documental`: sem NENHUM insumo legível (0 docs + sem texto colado + sem processo) retorna `precisaDocumentos` em vez de um laudo inteiro de "não consta — confirmar". `Analise.jsx` mostra card pedindo para anexar **matrícula + edital**.

---

## 2. Estado das fontes (validação de confiabilidade do Score)

| Camada | Peso | Fonte | Confiabilidade |
|---|---|---|---|
| Margem | 40% | desconto vs. avaliação Caixa **(ou vs. mercado real, se houver análise)** | Baixa sem análise → **corrigida** com análise |
| Localização | 25% | OSM local (osmium) | Média-alta, só nível-endereço (~4.077) |
| Perfil | 15% | modalidade+tipo (heurística) | Média |
| Jurídico | 10% | IA sobre documentos | Alta com docs / 0 sem |
| Financeiro | 10% | fórmula sobre desconto vs. avaliação | Baixa (não é o ROE real — ver pendência) |

---

## 3. Data CEF — mantido o ritmo (decisão do dono)
- Datas CEF sobem via cron (GitHub Actions a cada 3h): **478→852→1.067** (meta ~9.655). Gargalo = Caixa **bloqueia o IP do runner após ~3 min/execução** → acumula ao longo dos dias. **Cron NÃO foi acelerado** (a pedido; "em breve regulariza").
- On-demand já existe (`enriquecer-lote` + fallback Bright Data), mas é hit-or-miss (Caixa devolve 504/bloqueio intermitente).
- Edital CEF: **0 de 32.734** têm PDF real; `link_edital` aponta pro site (captura é best-effort no scrape de datas). Bright Data **Scraping Browser** (navegador + IP residencial) resolveria data+edital juntos — depende de habilitar a zona e passar a URL de conexão.

---

## 4. Aprovado para a PRÓXIMA sessão (redesenho da análise em agentes)

Direção aprovada pelo dono: **segmentar a IA por função** (aprendizado específico), unificando vários pedidos:
1. **Agente Mercadológico + Viabilidade Financeira** (já é `gerar-analise`).
2. **Agente Documental + Jurídico** (já é `gerar-documental`, com o loop de aprendizado do jurídico).
3. **Agente de Defesa/Parecer final** = **3º documento**, gerado a partir dos DOIS relatórios anteriores (tira a "etapa 6/laudo" de dentro do mercadológico).
4. **Captação de lotes NÃO é IA** — vira função do **monitor do sistema/indicadores** (monitora, aprende, busca soluções de scraping).

### Itens do redesenho dos relatórios (pendentes)
- **#4 Legendas das siglas**: ícone ⓘ com tooltip para VPL, TIR, TMA, ROE, Payback, Múltiplo do Capital (em `RelatorioPDF.jsx` e na tela `Analise.jsx`).
- **#5 Lance com disputa < sem disputa**: quando o teto viável < lance mínimo, trocar a coluna enganosa por aviso "Sem margem para disputa" (`RelatorioPDF.jsx` linhas ~98/122/145).
- **#6 Quadro-resumo no topo** de cada relatório (imóvel, lances das praças, valor pretendido de venda, forma de pagamento, **veredito aprovado/reprovado**) + corpo tópico a tópico.
- **#7 Laudo de viabilidade** como 3º documento (agente de defesa).

### Outros pendentes
- **Jurídico**: simplificar a tela de anexos (tirar campos que dão impressão de complexidade — a base do gate já está feita); melhorar a busca automática de **matrícula/edital** via Bright Data (o servidor não lê o PDF da Caixa por bloqueio de IP).
- **Score/Financeiro**: hoje `score_financeiro` (10%) ainda é a fórmula determinística do desconto; idealmente derivar do ROE real (a Margem já foi corrigida; o Financeiro é o próximo).

---

## 5. O que VALIDAR amanhã (deploy de hoje)
1. **Score**: abrir o imóvel de Sorocaba (`e51d44ab-3873-4815-9e73-380f85c5cfa7`), rodar o mercadológico; o card deve **cair para vermelho** (reprovado) em vez de 6,2.
2. **Jurídico**: gerar documental sem anexos → deve aparecer o card **"Anexe os documentos"**, não o laudo de "não consta".
3. **Mapa**: reabrir um imóvel (Ctrl+F5) → mapa deve renderizar.
4. **Usuários**: Neuma como "Investidor Pro" + selo PAGO.

---

## 6. Config que depende do dono (opcional)
- **Bright Data Scraping Browser** (para data+edital CEF em tempo real): habilitar zona no painel BD + passar a URL de conexão.
- Consultores ativos cadastrados (para o sorteio do SDR ter para quem distribuir).
