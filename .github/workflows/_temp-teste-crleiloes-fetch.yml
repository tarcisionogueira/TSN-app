name: _temp Teste fetch cru — crleiloes pesquisaAvancada

on:
  workflow_dispatch:

jobs:
  teste:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Teste
        run: node scripts/_teste-crleiloes-fetch.mjs
