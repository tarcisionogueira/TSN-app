# Handoff — 2026-07-06

Branch de trabalho: `claude/bidpro-brasil-resume-cf7ajx` (preview)
Status da branch: **6 commits à frente da `main`** — nada em produção ainda.

---

## ✅ Concluído hoje (na branch, buildado, ainda NÃO em produção)

### 1. Contradição "viável × inviável" no relatório mercadológico — CORRIGIDO
- **Sintoma:** o topo do relatório dizia "viável" e o texto do parecer dizia "inviável" no mesmo lote (ex.: Mairiporã).
- **Causa:** o servidor usava as métricas do snapshot do cliente, tiradas ANTES da pesquisa de mercado (valorMercado = 0), enquanto a tela recalculava ao vivo com o valor pesquisado.
- **Fix (`api/gerar-analise.js`):** recalcula as métricas no servidor com o valorMercado recém-pesquisado (mesma `calcularMetricasCenario` da tela) e usa essas métricas tanto no prompt do parecer quanto no veredito `analise_viavel`/`score_financeiro`.
- **Fix (`src/pages/Analise.jsx`):** usa `areaTerrenoM2` como fallback de área ao estimar o valorMercado (terrenos/lotes têm `areaM2 = 0` e ficavam sem valor).
- Commit: `f540c83`

### 2. Etapa 6 renomeada
- Era "Laudo de Viabilidade e Defesa da Arrematação" (colidia com o 3º relatório real). Agora "Parecer Executivo (Mercadológico + Viabilidade)".
- Commit: `f16797e`

### 3. Conversões do Google Ads — LIGADAS e com rótulos REAIS
- **Lacuna encontrada:** `trackPlanContratado` (conversão de assinatura paga) existia em `utils/gtag.js` mas **nunca era chamado** → nenhuma venda contava. Agora dispara em `confirmarPagamento()` (`Checkout.jsx`), único ponto de confirmação (redirect MP, polling Asaas, onPago do cartão). Guardado por preço > 0 e por ref anti-duplicidade. Commit: `b0a1da9`
- **Rótulos corrigidos** (os antigos, numéricos, eram placeholders):
  - `CONV_CADASTRO` → `AW-16850175262/ODg0CIfDvMscEJ6K5el-` (conversão "Cadastro concluído", categoria Inscrições). Commit: `1de579e`
  - `CONV_PLANO` → `AW-16850175262/73k7CIG38sMcEJ6K5eI-` (conversão "Plano Contratado BidPro", categoria Assinatura). Commit: `bc518f4`
- **Google Ads (feito pelo usuário):** duplicata "Cadastro BidPro" removida.

---

## ⏳ Pendente para amanhã (prioridade)

### A. Publicar as correções em produção
Duas correções esperam deploy: as **conversões** (só contam quando estiverem no site real) e a **contradição do relatório**.
Problema: a branch carrega JUNTO a **migração de URL** (commit `571a33d`, HashRouter→BrowserRouter), que **não pode ir para produção** antes do Supabase estar configurado.
**Decisão pendente do usuário:**
- **Opção A:** levar para a `main` só os commits de marketing/relatório (`f540c83`..`bc518f4`), deixando o `571a33d` na branch.
- **Opção B:** configurar o Supabase primeiro e publicar tudo junto.

### B. Migração de URL (pré-requisito da Opção B) — resolver amanhã
1. Supabase → Authentication → URL Configuration → **Redirect URLs**: adicionar `https://bidprobrasil.com.br/**`.
2. Conferir Site URL = `https://bidprobrasil.com.br`.
3. Testar no preview da Vercel: **login Google/OAuth, checkout, recuperação de senha, link de indicação (referral)**.
4. Só então publicar a branch inteira na `main`.

---

## 📌 Outros pendentes (backlog, sem urgência)

- **Meta Pixel:** scaffold instalado em `index.html` (inerte). Falta o **ID do Pixel** (Meta Events Manager) para substituir `000000000000000`.
- **OAuth "aparece TSN":** trocar o *App name* na tela de consentimento do Google Cloud (OAuth consent screen) de "TSN" para "BidPro Brasil".
- **`package.json`:** renomear `tsn-app` → `bidpro-brasil` (cosmético).
- **Verificação dos rótulos de conversão em produção:** após deploy + tráfego, confirmar no Google Ads que "Cadastro concluído" e "Plano Contratado BidPro" saem de "Inativo" para "Gravando".

---

## Notas de contexto
- As conversões só saem de "Inativo" quando disparam com campanha ativa + tráfego. Não é erro.
- A tag base do Google (`AW-16850175262`) já está no `index.html`; nenhuma tag adicional precisa ser colada no site — os eventos são disparados via código.
- Regra do usuário: **sem travessão (—)** em telas/relatórios lidos pelo cliente; código para "Claude do Chrome" sempre pedir retorno em **JSON texto puro** em bloco de código.
